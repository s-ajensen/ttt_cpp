#include "catch.hpp"
#include "bdd.hpp"
#include "board.hpp"

describe("Board", []{

  it("starts with no moves", [] {
    // Board b;
    // for (int i: b.cells) {
    //   should_eq(0, i);
    // }
  });

});
